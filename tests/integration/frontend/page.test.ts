import { describe, test } from "vitest";

describe("XxxPage", () => {
    describe("初期表示", () => {
        test.todo("初期表示時にxxxが表示される");
        test.todo("ローディング中はxxxが表示される");
    });

    describe("データ取得", () => {
        test.todo("マウント時にxxxAPIが呼ばれる");
        test.todo("取得成功時にxxxが表示される");
        test.todo("取得失敗時にエラー表示xxxが出る");
    });

    describe("データフロー", () => {
        test.todo("取得したxxxが子コンポーネントに渡される");
        test.todo("子コンポーネントのイベントで状態が更新される");
    });

    describe("ユーザー操作", () => {
        test.todo("xxx操作でxxxユースケースが実行される");
        test.todo("操作成功時に画面がxxxに遷移する");
    });

    describe("状態管理", () => {
        test.todo("xxx状態のとき、xxxコンポーネントが表示される");
    });
});
