import { describe, test } from "vitest";

describe("XxxComponent", () => {
    describe("props", () => {
        test.todo("xxxが渡された場合、xxxが表示される");
        test.todo("xxxが未指定の場合、デフォルト値xxxが使われる");
    });

    describe("描画", () => {
        test.todo("初期状態ではxxxが表示される");
        test.todo("条件xxxのとき、xxxが非表示になる");
    });

    describe("状態管理", () => {
        test.todo("xxx操作で状態xxxがxxxに変化する");
    });

    describe("インタラクション", () => {
        test.todo("xxxクリックでxxxイベントが発火する");
        test.todo("xxx入力でonChangeが呼ばれる");
    });

    describe("副作用", () => {
        test.todo("マウント時にxxxが実行される");
        test.todo("アンマウント時にxxxが実行される");
    });
});
